// Disabled intentionally.
// property_units.property_id now references clients(id), so the legacy
// portal_properties bridge is no longer needed for manual unit saves.
console.info("[property-units] legacy property bridge disabled.");
